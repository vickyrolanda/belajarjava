const loginBtn=document.getElementById('loginBtn');const usernameInput=document.getElementById('username');const passwordInput=document.getElementById('password');const messageP=document.getElementById('message');const logArea=document.getElementById('logArea');
function appendLog(text){const t=new Date().toLocaleTimeString();logArea.textContent+=`[${t}] ${text}\n`;logArea.scrollTop=logArea.scrollHeight}
loginBtn.addEventListener('click', async ()=>{const username=usernameInput.value.trim();const password=passwordInput.value;messageP.textContent='';if(!username||!password){messageP.textContent='Isi username & password (password bebas)';return}appendLog(`Mengirim login request untuk "${username}"...`);try{const res=await fetch('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username,password})});const data=await res.json();if(res.ok){appendLog(`Server: ${data.message}`);messageP.style.color='green';messageP.textContent=data.message;appendLog(`KEY: ${data.key}`);}else{appendLog(`Gagal: ${data.message}`);messageP.style.color='red';messageP.textContent=data.message}}catch(e){appendLog('Fetch error: '+e.message);messageP.style.color='red';messageP.textContent='Gagal menghubungi server'}});